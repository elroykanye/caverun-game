libgtk2.0-dev
libgtk-3-dev
libgtk-3-0
libgtk-4-dev
libgtk-4-1
pkg-config 
cmake 
clang
build-essential

conan install . --output-folder=build --build=missing



MAIN WINDOW
    - main box (vertical)
        -- topbar box (horizon)
        -- grid box (a grid)
        -- bottom bar box (horizon)